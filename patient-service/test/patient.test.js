describe('Simple Patient Service Tests', () => {
  it('should always pass', () => {
    expect(true).toBe(true);
  });

  it('should test basic math', () => {
    expect(1 + 1).toBe(2);
  });

  it('should test string operations', () => {
    expect('hello').toContain('hell');
  });
});